<h2>List of Subjects</h2>
<ul class="list-group">
<% @subjects.each do |s| %>
  <li class="list-group-item justify-content-between align-items-center">
    <%= link_to s.name, grades_path(form_id: @form, subject_id: s.id) %>
  </li>
<% end %>
